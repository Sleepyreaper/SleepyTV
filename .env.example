# Copy to ".env" (same folder as compose.yaml) and fill in YOUR NAS credentials.
# These are used only to mount \\sleepynas\PlexMediaServer into Jellyfin.
# Do not commit the real .env.
NAS_USER=your-nas-username
NAS_PASS=your-nas-password
